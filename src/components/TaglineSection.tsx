const TaglineSection = () => {
  return (
    <section
      id="tagline"
      className="relative isolate px-6 pt-14 lg:px-8 min-h-[30dvh] md:min-h-[40dvh] text-center"
    >
      tag line
    </section>
  );
};

export default TaglineSection;
